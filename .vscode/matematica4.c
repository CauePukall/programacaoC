#include <stdio.h>

int main(){
    int x = 10;
    float y = 3.5;
    float soma = x + y;
    float diferenca = x - y;
    float produto = x * y;
    float quociente = x / y;

    printf("Soma: %.2f\n", soma);
    printf("Diferenca: %.2f\n", diferenca);
    printf("Quociente: %.2f\n", quociente);
    printf("Produto: %.2f\n", produto);



}